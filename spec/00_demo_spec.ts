describe('Writing a Spec', () => {
    it('is easy when you know how!', () => {

        // GIVEN - ARRANGE
        const a = 10, b = 20;
        //WHEN - ACT
        const answer = a + b;
        //THEN - ASSERT
        expect(answer).toBe(30)
    });
});